# Start the completed REST API on the trusted loopback interface.
uv run jj serve --host 127.0.0.1 --port 8000

# In another terminal, verify liveness and PostgreSQL readiness.
curl -fsS http://127.0.0.1:8000/api/v1/health/live
curl -fsS http://127.0.0.1:8000/api/v1/health/ready

# Follow the complete sandbox walkthrough in README.md to exercise every endpoint.

# After live smoke validation, apply the canonical REST story completion update in:
# docs/development/backlog-session-update-plan-2026-07-10.md
